import { ListTasksFilteredComponent } from './list-tasks-filtered.component'

describe('ListTasksFilteredComponent', () => {
  it('should mount', () => {
    cy.mount(ListTasksFilteredComponent)
  })
})