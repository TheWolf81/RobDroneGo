import { ListRequestedTasksComponent } from './list-requested-tasks.component'

describe('ListRequestedTasksComponent', () => {
  it('should mount', () => {
    cy.mount(ListRequestedTasksComponent)
  })
})